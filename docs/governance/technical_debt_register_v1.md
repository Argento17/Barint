# Technical Debt Register v1

Scope: debt affecting comparison-template rollout to bread and snacks.

| Priority | Issue | Location | Impact | Urgency | Recommended action |
|---|---|---|---|---|---|
| HIGH | Maadanim reference files are untracked at audit time | `git status --short` lists `docs/comparison_ui_reference_v1.md`, `src/app/hashvaot/maadanim/`, shared comparison components, `src/data/comparisons/`, registry files | Rollout baseline may disappear or diverge across branches | Before any rollout branch | Commit/stabilize the reference implementation and docs before deriving bread/snack work. |
| HIGH | Registry only supports Maadanim | `src/lib/comparisons/registry/types.ts` | Bread/snack cannot use `createComparisonCategoryRoute()` or registry API cleanly | Before adding second category | Widen `ComparisonCategoryId` only when each category has a valid definition and VM data adapter. |
| HIGH | Bread is still a standalone comparison dashboard | `src/components/comparisons/bread-comparison-dashboard.tsx`, `src/app/compare/bread-comparison/page.tsx` | Bread rollout would not match Maadanim reference IA, mobile frame, product row, or expansion model | Before bread rollout | Create a bread `BariProductVM` data adapter and route through the reference shelf stack. |
| HIGH | Snack production routes are disabled | `src/app/hashvaot/snack-bars/page.tsx`, `src/app/compare/snack-bars/page.tsx` | Snack comparison cannot roll out from current routes | Before snack rollout | Replace disabled routes with canonical route or documented redirects after data adapter exists. |
| HIGH | Snack current engine exposes banned algorithm vocabulary | `src/lib/comparisons/snack-product-detail.ts`, `src/components/snack/product-card-grid.tsx`, `src/components/snack/map-section.tsx`, `src/lib/comparisons/snack-page-data.ts` | Violates reference rule against NOVA/caps/score mechanics in comparison UI | Before snack rollout | Do not reuse snack engine/detail copy for reference comparison route; map snack data to v2 pre-authored VM fields. |
| HIGH | Bread/snack data are not canonical VM corpora | `src/lib/comparisons/bread-types.ts`, `src/lib/comparisons/snack-types.ts`, `src/data/bread-retail-curated.json`, `src/lib/comparisons/snack-page-data.ts` | Shared `ProductTable` and `ExpansionSection` cannot consume them directly | Before category rollout | Produce repo-owned `src/data/comparisons/{category}_frontend_v2.json` or adapters that output `BariProductVM[]`. |
| MEDIUM | `BariCategoryPageVM` is defined but not used by operational category pages | `src/lib/view-models/index.ts`, `src/lib/comparisons/registry/types.ts` | Two page contracts can drift | Before 3+ categories | Decide whether operational registry shape replaces or implements `BariCategoryPageVM`. |
| MEDIUM | Filters are not page-VM driven | `src/lib/comparisons/maadanim-shelf-filters.ts`, `docs/comparison_ui_reference_v1.md` | Every category may grow bespoke filter ids, counts, and match rules | Before 3+ categories | Keep category filter modules short-term; plan a single filter contract when stable. |
| MEDIUM | Route families compete | `/hashvaot/maadanim`, `/compare/bread-comparison`, disabled snack routes | Confusing QA matrix and inconsistent links | Before public rollout | Define canonical comparison route family and redirect policy. |
| MEDIUM | Route metadata duplicated for Maadanim | `src/app/hashvaot/maadanim/page.tsx`, `src/lib/comparisons/maadanim-page-data.ts` | Title/description drift | Low effort before rollout | Use one metadata export consistently. |
| MEDIUM | Shared loader lacks runtime validation | `src/lib/comparisons/corpus.ts` | Bad JSON can fail in UI or silently hide data issues | Before multiple corpora | Add validation or a QA script for required VM fields, unique ids, expansion fields, and metadata. |
| MEDIUM | Shared expansion overrides corpus confidence label | `src/components/shared/expansion-section.tsx` | Pre-rendered `expansion.confidenceLabel` is not always rendered verbatim | Before categories with custom confidence wording | Decide whether shared labels or corpus labels are authoritative. |
| MEDIUM | `fiber` exists in `BariNutritionVM` but is not displayed | `src/lib/view-models/index.ts`, `src/components/shared/expansion-section.tsx` | Bread/snack may need fiber in technical block; current shared grid hides it | Before bread rollout if fiber is part of required technical data | Confirm whether fiber should remain hidden or be added to reference technical grid. |
| MEDIUM | Image host config may not cover new categories | `next.config.ts` | New corpus images may fail under Next Image | Before importing bread/snack corpora | Audit image URL hosts and add approved remote patterns if needed. |
| MEDIUM | Multiple score display components remain | `src/components/shared/score-chip.tsx`, `src/components/snack/snack-score-chip.tsx`, `src/components/comparisons/bari-grade-badge.tsx` | Visual/semantic drift for score and null-score states | Before route consolidation | Scope legacy score components to legacy/blog views or consolidate comparison routes on `ScoreChip`. |
| MEDIUM | Dev preview and QA capture path still target `/dev/preview` | `src/app/dev/preview/page.tsx`, `scripts/capture-maadanim-expansions.mjs` | QA can pass preview while production route regresses | Before relying on screenshots | Prefer production-route smoke/capture scripts for rollout signoff. |
| MEDIUM | `ComparisonShelfPage` remounts `ProductTable` when expanded id changes | `src/components/comparisons/comparison-shelf-page.tsx` | Can reset table state and cause scroll churn on long filtered lists | Before large corpora | QA with 200+ rows before changing behavior. |
| LOW | `_calibration` remains in repo JSON | `src/data/comparisons/maadanim_frontend_v2.json`, `src/lib/comparisons/corpus.ts` | Loader strips it, but raw repo artifact contains internal field | Hygiene before final data freeze | Strip internal fields at export time if possible. |
| LOW | Unused generic `ComparisonProduct` type remains | `src/lib/comparisons/comparison-product.ts` | Confuses canonical type ownership | Opportunistic cleanup | Retire if no imports remain after migration. |
| LOW | Hashvaot index is manually composed | `src/app/hashvaot/page.tsx` | Category cards/links can drift from registry | Before many categories | Consider registry-driven listing after bread/snack are on registry. |
| LOW | Legacy milk comparison remains separate | `src/components/comparisons/milk-comparison-page.tsx`, `src/lib/comparisons/milk-types.ts` | Adds architectural drift but outside current bread/snack mission | Later | Leave unchanged unless milk is scheduled for template migration. |

## Not Verified Items

| Priority | Issue | Location | Impact | Urgency | Recommended action |
|---|---|---|---|---|---|
| MEDIUM | `DISTORTION-001` documented but not implemented | Not found by audit search in `src`, `docs`, or `scripts` | Unknown QA/design risk | Before rollout signoff | Locate the documentation source or add explicit tracking note; do not assume implementation exists. |
| MEDIUM | External consumers of legacy routes | `/compare/bread-comparison`, `/compare/snack-bars`, `/hashvaot/snack-bars` | Redirect/removal could break inbound links | Before route cleanup | Check analytics/sitemap/nav references before retiring routes. |
