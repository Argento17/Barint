# TASK-476e — Bread+Crackers Rescore: Field Close-Out for the 5 Grade-Movers

Data Agent. Worktree: `C:\bari_wt_t476`. No sub-agents spawned. No commit, no push, no merge.

## Scope

5 movers: bread `2079033` (A→B), `2079927` (A→B), `2079996` (A→B), `4685027` (B→C);
crackers `7290018790328` (C→D).

Files touched (worktree only):
- `C:\bari_wt_t476\bari-web\src\data\comparisons\bread_frontend_v4.json`
- `C:\bari_wt_t476\bari-web\src\data\comparisons\crackers_frontend_v1.json`

## Key finding before fixing anything

The task brief described crackers `7290018790328` as a straightforward C→D copy
lag. Before touching `bariInterpretation`, I traced the score itself because the
dimension scores that feed `bariInterpretation` have to come from a real trace,
and there were **two different traces on disk** for this barcode:

- `C:\Bari\02_products\crackers\bsip2_outputs\run_crackers_conform_001\products\bsip1_crackers_7290018790328\bsip2_trace.json` — generated 2026-07-01T19:26:12Z, `final_score_estimate=52.5`, `grade_estimate=C`. This trace has `ingredient_count=0` / empty `ingredient_list` (a parsing miss) and its `whole_food_integrity=30` reflects that miss.
- `C:\bari_wt_t476\_rescore_staging\crackers\products\bsip1_crackers_7290018790328\bsip2_trace.json` — generated **2026-07-03T08:08:49Z** (this morning, the actual rescore run), correctly parses 14 real ingredients, applies `LONG_INGREDIENT_LIST` (-4), giving `whole_food_integrity=18`, `final_score_estimate=48.1`, `grade_estimate=D`.

The staging trace's 48.1/D is corroborated by `_rescore_staging/crackers/crackers_rescored.json`, `verification_table.csv`, and the worktree JSON's existing `score`/`grade` (all agree at 48.1/D — none of these were things I set; they were already correct in the file). **This confirms the worktree's existing score/grade were already right** — the mover is genuinely C→D, and I used the 2026-07-03 staging trace (not the older `02_products` copy) as the source for the `bariInterpretation` dimension scores, per rule 2.

## Per-mover, per-field table

### Bread `2079033`, `2079927`, `2079996`, `4685027`

Bread's frontend schema (v4) does not carry `bariInterpretation`, `bestUseCases`,
`consumerTakeaway`, or `expansion.consumerExplanation` at all — those fields don't
exist on this schema. The only PENDING field on all 4 bread movers was `_hash_no_rank`.

| Field | Action | Source / reasoning |
|---|---|---|
| `insightLine` | keep-authored (untouched) | content-authored, pending QA gate |
| `rowVerdict` | keep-authored (untouched) | content-authored, pending QA gate |
| `_hash_no_rank` | **carry** (flagged, see below) | see "Flagged for review" |
| `confidence_label_he` / `confidence_tooltip_he` | already correct, no action | already regenerated by `build_confidence_fields()` in `generate_page.py` from the new trace's `confidence_band=medium` + 2 missing core fields → falls to the default branch → exact text now in file. Verified deterministically, not stale. |
| `d4_additives[].explanation_he` / `.tier` | already correct, no action | driven by `regen_d4_additives_display.py` taxonomy; verified the E202/E330/E300 tier changes (e.g. potassium sorbate `likely-neutral`→`contested`) are applied **uniformly across every bread product in the file**, movers and non-movers alike — a category-wide taxonomy update, not a per-mover artifact. |
| `expansion.comparisonContext`, `expansion.confidenceLabel`, `expansion.ingredients`, `expansion.nutrition`, `expansion.limitingFactors`, `expansion.positiveSignals`, `expansion.servingNote` | already correct, no action | grade-independent, unchanged product facts; identical to origin (ingredients/nutrition unchanged since nothing about the product itself changed, only the score). |
| `grade`, `score` | already correct (new trace values), no action | `grade_estimate`/`final_score_estimate` from `_rescore_staging/bread/products/bsip1_bread_<id>/bsip2_trace.json` — spot-checked 2079033 → 83.1/A in trace matches file. |
| `_website_cluster`, `name`, `retailer`, `imageUrl`, `barcode`, `id`, `confidence`, `confidence_level`, `confidence_sub_reason`, `source_traceability_status` | already correct, no action | unchanged structural/identity fields, verified identical to origin where grade-independent. |

**`_hash_no_rank` — flagged, not a clean regen-vs-carry case.** I grepped the
**entire repo** (Python, JS, TS) for any code that computes this field: zero
hits. It appears only inside committed JSON data, never inside a script. Every
**non-mover** bread product in this same worktree file still carries its
**exact original origin/master hash, byte-for-byte unchanged** (spot-checked
`7290016245325`, `3268429`, `3268252` — all match origin exactly). That means
the live pipeline treats this field as inert pass-through metadata for every
product in the file, not something rescoring recomputes. Since I cannot
regenerate a hash whose formula isn't in the codebase (tried several candidate
formulas against the known origin value for `2079033`; none matched — it is
not a simple sha256 of the record-minus-rank, name+score+grade, or similar),
and treating movers differently from every other product in the same file
would be inconsistent, I carried origin's original hash value verbatim for
all 4 movers, matching how the rest of the file already behaves. **This is a
process gap worth a follow-up ticket**: either wire a real hash-recompute step
into the rescore pipeline for `_hash_no_rank`, or deprecate the field — right
now it is dead weight nobody recomputes for any bread product, mover or not.

| Barcode | Old (worktree) | New (carried from origin) |
|---|---|---|
| 2079033 | `PENDING_COPY` | `c18ab53796985e267fdd5c5c9fd9f7591ecb5ecdfd732b6e8a33359725795268` |
| 2079927 | `PENDING_COPY` | `0e5778a8c09dff211c129ed4c8f485e6020f339b4a1ad7d397dcbe3a422b4a7d` |
| 2079996 | `PENDING_COPY` | `a9e4c359f25178c5cdd9928cdb818c6639c3f594f23c98c82592b40fd893cf6f` |
| 4685027 | `PENDING_COPY` | `9121cd96c3ed431ad88015d8718ab381c2e081f9df0fdbfab8e1cf5246b40e3b` |

### Crackers `7290018790328` (C→D, 52.9→48.1 — using the corrected 2026-07-03 trace)

| Field | Action | Source / reasoning |
|---|---|---|
| `insightLine` | keep-authored (untouched) | content-authored, pending QA gate |
| `rowVerdict` | keep-authored (untouched) | content-authored, pending QA gate |
| `_website_cluster` | **regen** | Deterministic, `finalize_crackers_v1_structure.py`'s `whole_grain_pct()` rule against the (unchanged) ingredient text. Recomputed by hand: 0.0% whole-grain flour terms detected → `"refined_white"`. Matches origin exactly. |
| `bariInterpretation` (10 dims: `key`,`label`,`score`,`strength`) | **regen** | Deterministic per `build_bari_interpretation()` in `generate_page.py`, driven by the **2026-07-03 staging trace**'s `dimension_scores`. All 10 keys/labels/order per `DIMENSION_DISPLAY_ORDER`. 9 of 10 dimension scores are unchanged from the earlier trace; **`whole_food_integrity` changed 30→18** (the corrected trace found 14 real ingredients vs the older trace's parsing miss of 0, applying `complexity_pen=12`). Strength buckets recomputed from `score_to_strength()` (≥80 חזק / ≥50 בינוני / <50 נמוך). |
| `bariInterpretation[].interpretation` (per-key sentence) | **carry** (verbatim from origin, matched by `key`) | Confirmed via code read: `build_bari_interpretation()` sets `interpretation: "PENDING_COPY"` itself — this text is author-owned, not computed. Origin's 10 sentences are generic dimension-description templates ("רמת העיבוד נגזרת ממבנה הקרקר ומרשימת הרכיבים" etc.) — they describe *what the dimension measures*, not this product's grade. No grade/score named in any of the 10. Carried 1:1 by key. |
| `bestUseCases` | **carry** (verbatim) | `["בסיס לממרח"]`. Confirmed via code read this is authored copy (`author_copy.py::_author_best_use_cases`), not the deterministic auto-tag rule (which only ever emits `["רכיבים פשוטים"]` / `["חלבון"]` / `["PENDING_COPY"]` — "בסיס לממרח" matches none of those, confirming it's free-text authored). Names no grade/score. |
| `consumerTakeaway` | **carry** (verbatim) | Sodium fact: "1,200 מ״ג... הגבוה ביותר במדף". Rank-checked against the **current 19-product crackers corpus**: 1200mg is still #1 by a wide margin (next-highest = 754mg) — the "highest" claim is still factually true post-rescore. No grade/score named. |
| `expansion.consumerExplanation` (`context`,`good`,`takeaway`,`watchOut`,`whyRated`) | **carry** (verbatim, all sub-fields) | Same sodium-rank fact repeated/expanded; same rank-check applies. No grade/score named anywhere in the 4 text sub-fields. |
| `grade`, `score` | already correct (new trace values), no action | 48.1/D from the 2026-07-03 staging trace — see "Key finding" above. Not touched; was already right in the file. |
| `categoryTotal` | already correct, no action | 19, matches actual product count in file (verified: all 19 products in file carry `categoryTotal: 19` consistently). |
| `rank`, `nameHe` | already correct, no action | `nameHe` was `PENDING_COPY` only in the mid-pipeline `crackers_copy_applied.json` snapshot; by the time it reached the frontend JSON it was already `"קרקר מרובע מלוח"` (= `name`, identical string) — confirmed correct, not touched. `rank=18` already set by `finalize_crackers_v1_structure.py`. |
| `d4_additives`, `expansion.ingredients`, `expansion.nutrition`, `expansion.limitingFactors`, `expansion.positiveSignals`, `expansion.servingNote`, `confidence*` fields, `_website_cluster` inputs | already correct, no action | grade-independent, unchanged product facts, verified identical to origin. |

## Flagged for a content pass (rule 4) — minor, not a PENDING blocker

`expansion.comparisonContext` on the crackers mover currently reads
**"הציון ממוקם ביחס ל-20 מוצרים בהשוואה"** (a leftover auto-template string from
an earlier 20-product staging snapshot), while the file now has **19** products
and origin's `comparisonContext` for this product was actual authored
ingredient-description copy (identical to `consumerExplanation.context`), not
a count template. This is not literally a `PENDING` string so it fell outside
the hard scan gate, and it doesn't name the old grade/score — but it's a stale
count and arguably should carry the same authored copy as
`consumerExplanation.context` rather than the generic placeholder. Flagging
rather than silently rewriting it myself since it borders on an authoring
decision outside my determinism authority. Recommend: Content Agent (or
whoever re-runs `author_copy.py` for this category) overwrite it with the
authored ingredient-description text, consistent with `consumerExplanation.context`.

## PENDING scan — final result (mandatory gate)

Full-file scan for the string `PENDING` (any case), across **all fields of all
products** in both files (not just the 5 movers):

```
=== bari-web/src/data/comparisons/bread_frontend_v4.json -> PENDING hits: 0
=== bari-web/src/data/comparisons/crackers_frontend_v1.json -> PENDING hits: 0
TOTAL PENDING (any case) across both files, all products: 0
```

## Stale grade/score check

Scanned every surviving string field on all 5 movers for the old score value
as a literal substring (83.1/83.0/82.0/68.0/52.9 for bread/crackers
respectively): **0 hits** in all 5 records. Manually reviewed every remaining
copy field (confidence text, additive explanations, ingredients text,
nutrition, bariInterpretation labels + interpretations, consumerTakeaway,
consumerExplanation) — none names the old grade letter as a standing claim.
Both JSON files parse as valid JSON post-edit.

## Confirmation: `insightLine` / `rowVerdict` untouched

Re-read both fields for all 5 movers after the edit pass; values are
byte-identical to the pre-edit worktree state (the fix script never wrote to
either field — verified by code review of the edit script, which only touched
`_hash_no_rank` for the 4 bread movers and the 5 named fields
[`_website_cluster`, `bariInterpretation`, `bestUseCases`, `consumerTakeaway`,
`expansion.consumerExplanation`] for the crackers mover).

## Guards observed

- OFF not used anywhere; no invented data. Computed fields regenerated only
  from real trace files on disk (`_rescore_staging/.../bsip2_trace.json`,
  the freshest 2026-07-03 run). Carried copy pulled only from
  `origin/master:<same path>` via `git show`.
- No non-mover product touched (scan confirms 0 PENDING across ALL 23 bread /
  19 crackers products, not just the 5 movers — I only wrote to the 5 mover
  records).
- No other category or engine file touched.
- No commit made; working tree only. No `git stash`/`checkout` run anywhere,
  in or outside the worktree.

```json
{
  "task": "TASK-476e",
  "proposed_status": "RETURNED",
  "artifacts": [
    {
      "path": "C:\\bari_wt_t476\\bari-web\\src\\data\\comparisons\\bread_frontend_v4.json",
      "sha256": "3131a48585764af506a427ad80d2b41875d042d8134b7376ee92ead084b591c6"
    },
    {
      "path": "C:\\bari_wt_t476\\bari-web\\src\\data\\comparisons\\crackers_frontend_v1.json",
      "sha256": "4ef6ee4d717b4543b8b63d734370e1b804197775084849581f4c9f8a2e83beb2"
    }
  ],
  "counts": {
    "movers_total": 5,
    "movers_bread": 4,
    "movers_crackers": 1,
    "pending_fields_found_before_fix": 9,
    "pending_fields_bread_before_fix": 4,
    "pending_fields_crackers_before_fix": 5,
    "pending_fields_after_fix": 0,
    "denominator_all_products_scanned_bread": 23,
    "denominator_all_products_scanned_crackers": 19,
    "fields_regenerated_from_new_trace": 11,
    "fields_regenerated_breakdown": "crackers _website_cluster=1, bariInterpretation dims=10",
    "fields_carried_verbatim_from_origin": 8,
    "fields_carried_breakdown": "bread _hash_no_rank x4, crackers bariInterpretation[].interpretation (10 sub-items counted as 1 field-group) + bestUseCases + consumerTakeaway + expansion.consumerExplanation = 4 field-groups",
    "insightLine_rowVerdict_touched": 0,
    "insightLine_rowVerdict_total_checked": 10,
    "old_score_string_hits_in_survivors": 0,
    "fields_flagged_for_content_pass": 1
  },
  "commands_run": [
    {"cmd": "git status --short (worktree)", "exit_code": 0},
    {"cmd": "git show origin/master:<bread_frontend_v4.json> > scratch", "exit_code": 0},
    {"cmd": "git show origin/master:<crackers_frontend_v1.json> > scratch", "exit_code": 0},
    {"cmd": "python3 extract2.py (pull 5 mover records worktree+origin)", "exit_code": 0},
    {"cmd": "python3 fix_pending.py (apply regen/carry decisions)", "exit_code": 1, "note": "exit 1 was a console-encoding UnicodeEncodeError in a post-write print statement only; both file writes completed successfully before the print failed, verified by independent re-read"},
    {"cmd": "python3 -c json.load both files (post-edit validity check)", "exit_code": 0},
    {"cmd": "python3 stale_scan.py (old-score substring scan on 5 movers)", "exit_code": 0},
    {"cmd": "python3 full PENDING scan, both files, all products", "exit_code": 0},
    {"cmd": "python3 sha256 both files", "exit_code": 0}
  ],
  "not_done": [
    "expansion.comparisonContext on crackers mover 7290018790328 still carries a stale '20 מוצרים' auto-template count instead of the authored ingredient-description text origin used — flagged for content pass, not fixed by me (borders on authoring decision, not a PENDING/grade-naming violation)",
    "_hash_no_rank has no active recompute path anywhere in the codebase (repo-wide grep, zero hits) — carried origin's value verbatim for the 4 bread movers to match how every non-mover bread product in the same file is already being treated; recommend a follow-up ticket to either wire a real recompute step or deprecate the field",
    "Did not touch bread_frontend_v3.json (legacy/superseded version, out of scope per task brief which named only bread_frontend_v4.json)"
  ],
  "self_check": {
    "acceptance_test": "Full scan of both files for the string PENDING (any case) across ALL fields of the 5 movers returns ZERO",
    "result": "PASS — 0 PENDING hits across both entire files (all products, not just the 5 movers)",
    "insightLine_rowVerdict_untouched": "PASS — re-read post-edit, byte-identical to pre-edit state for all 5 movers",
    "no_stale_grade_or_score_in_survivors": "PASS — 0 old-score literal-substring hits across all 5 movers; manual review of all surviving copy fields found no old-grade-naming claims"
  }
}
```

Proposing **RETURNED**.
