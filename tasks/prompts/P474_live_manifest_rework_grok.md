# P474 / TASK-466 REWORK of P473: the manifest must derive from the APP ROUTES, not the 7-entry registry (route: C1-GROK)

## 1. The verification failure (read carefully — this inverts your P473 conclusion)
Your manifest derived from `comparisonCategoryRegistry` (7 entries) and called brined-cheeses/cakes/juices/etc. "catalog-only slugs ... no route." **That is factually wrong: they are LIVE pages.** `git ls-tree origin/master bari-web/src/app/hashvaot/` shows 19 route directories — brined-cheeses, cakes, chocolate-bars, chocolate-tablets, cookies-coffee, hard-cheeses, juices, milk-comparison, protein-bars and more are bespoke `page.tsx` routes OUTSIDE the registry (the "bespoke-adapter disease"). The orchestrator production-verified `bari.digital/hashvaot/brined-cheeses` serving 270KB TODAY. Your "intentional narrowing" of conformance `--all` from 18 config stems to 7 would REMOVE most live categories from the safety net — the exact class this task exists to kill. The registry's blind spot IS the defect; a manifest derived from it re-encodes the defect.

## 2. Rework objective (same worktree `C:\bari_wt_t466`, branch `feat/task466-live-manifest`, on top of cd20ca4e)
1. **Source of truth = the app directory.** Enumerate every `bari-web/src/app/hashvaot/*/page.tsx` (plus `/catalog`). For each route, statically resolve its data chain: page.tsx → page-data TS (`src/lib/comparisons/*-page-data.ts` or inline imports) → `src/data/comparisons/*.json`. The comparisonCategoryRegistry becomes ONE annotation (`in_comparison_registry: bool`), NOT the filter.
2. **Classify every route** in the manifest: `comparison` (has a comparison JSON), `hub` (supermarket, the /hashvaot index), `other-vertical` (magnesium, supplements, personal-care, raw-foods — no BSIP comparison JSON; include with `type` marker and no config mapping). A comparison route with NO resolvable JSON = `gaps`, loudly.
3. **Config mapping:** map each comparison category to its `03_operations/page_generator/configs/*.json` stem where one exists (bread→bread.json etc.; milk-comparison→milk.json; note stem-vs-route naming mismatches explicitly). A live comparison page with NO config = a `gaps` entry (that IS a safety-net hole — headline it). A config with no live page stays an orphan WARN.
4. **Conformance wiring:** `conformance.py --all` = manifest's comparison categories that have a config stem. Acceptance: the resulting set must be a SUPERSET-or-equal of the old 18-stem set MINUS provable non-pages (crackers_frontend_discards_v1, snacks_task413_staging and other staging/discard stems — justify each exclusion by showing no route imports them). Expected magnitude: ~16 comparison categories. Run conformance --all on the new set; report per-category conform/non-conform (pre-existing HARD-3s stay pre-existing — list them).
5. **Keep:** `--check` drift mode (incl. --simulate-drift), the CI step, `--categories` override. Regenerate the committed `live_manifest.json`.
6. **Hygiene:** `git checkout -- 03_operations/reports/regression/*.md` (your P473 run left generated report churn uncommitted — do not commit report outputs; if the repo-relative REPORT_ROOT now makes every regression run dirty the tree, add those two report paths to .gitignore and say so). Also re-run `run_router_regression.py` and explain in one paragraph why it exits 1 locally (dairy_flavor_contamination_biscuit) while the SAME step passed green on CI at this very commit (6284546a) — env/python-version/report-state difference? Diagnose, report, do NOT fix the regression itself.

## 3. Boundaries
Tooling only; no scoring behavior, no data JSONs, no consumer strings. OFF ban absolute. CI must stay green (workflow step included). No C:\Bari literals (CI = Linux). You are the EXECUTOR — do NOT spawn subagents.

## 4. Return
`tasks\returns\P474_contract.md` (NOT P474_return.md). Full manifest table (every route, type, JSON, config, gaps), the old-18 vs new set reconciliation with per-exclusion justification, conformance per-category results, the local-vs-CI router-regression explanation, commands + exit codes, real sha256s, counts with denominators + distribution markers. Self-gate: `python 03_operations\validators\validate_return.py --md tasks\returns\P474_contract.md --root C:\bari_wt_t466` exit 0 (PowerShell). Commit code + contract; leave tree clean. Propose RETURNED.
