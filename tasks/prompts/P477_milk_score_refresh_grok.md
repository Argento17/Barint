# P477 / TASK-468 milk score refresh: align published milk_frontend_v1 to the current engine (route: C1-GROK)

## 1. Context / baseline
- You are ALREADY in isolated worktree `C:\bari_wt_t468`, branch `refresh/task468-milk-scores`, cut from origin/master `06f85de4`. Repo root = this worktree. Never touch `C:\Bari` (read-only reference). Commit here; NO push/PR/deploy.
- Owner sanctioned this published-score change (tripwire-1 engaged 2026-07-02); the owner's merge click is the final gate. Read FIRST: `C:\Bari\tasks\TASK-468.md`, `tasks/closed/TASK-429.md` (canonical-invocation law), `03_operations/page_generator/provenance/hard_cheeses_canonical_invocation_v1.md` (the recipe pattern), `03_operations/page_generator/configs/milk.json` (MILK_CANONICAL_FLAGS — note its `corpus_dirs`/`run_products_dir`/`baseline_json` are C:\Bari-absolute; resolve them INSIDE this worktree), and `03_operations/page_generator/rescore_all.py` lines ~366-430 (C10 milk diagnostic, v2 — restores canonical flags + reloads engine before scoring).
- Known state (from TASK-429 DoD-5 + C10 at recent HEADs, flags-off): published `bari-web/src/data/comparisons/milk_frontend_v1.json` (18 products) reproduces EXACTLY for 16/18; two drifters vs current engine: `bsip1_7290110324926` (+0.2) and `bsip1_7290110325619` (+4.1). Cause class per TASK-429: post-publication data/engine fixes (NOT an invocation gap). This task re-baselines the published page to the current engine.

## 2. Objective — four steps, each gated
**Step A — reproduction gate (mandatory, BLOCKED on failure).** Run the canonical milk invocation (config-driven: MILK_CANONICAL_FLAGS restored + engine reload, corpus `03_operations/bsip1/run_milk_002/output`, per the C10 v2 code path — reuse its logic or the config runner, do NOT hand-roll flags) and compare per-barcode score+grade to the published JSON. Acceptance: exactly 16/18 byte-exact AND the drift set == exactly {7290110324926: +0.2±0.05, 7290110325619: +4.1±0.05}. Any other product moving, or a different delta → STOP, emit the per-product diff table, propose BLOCKED (the drift census is stale and the owner decision was made on wrong numbers).
**Step B — root-cause the +4.1.** Diff the fresh trace of 7290110325619 against its stored published-era trace (`02_products/milk_and_alternatives/intelligence_bsip2/run_006_shelfrel_refreeze/products/`) — name the exact mechanism that moved it (which signal/cap/weight changed, which engine commit or data fix, EV id if applicable). Same for the +0.2 mover (briefer). No hand-waving: the PR body needs "it moved because X".
**Step C — refresh candidate (rebuild-from-live method).** Start from the EXACT live JSON; swap ONLY score + grade from the fresh traces for the movers; recompute `rank` for all products (competition ranking, stable live-order tiebreak); regenerate any `_meta` fields derived from scores/grades/ranks from the FINAL products; every other byte (copy, nutrition, ingredients, schema) identical. Report the full movement table: barcode, name, score old→new, grade old→new, rank old→new for ALL 18 (non-movers included, showing rank shifts). Headline whether either mover crosses a grade boundary. Emit the Rule-7 flat table (barcode, score, grade, binding caps, context flags).
**Step D — copy-impact audit (REPORT-ONLY — owner description freeze is ACTIVE).** Sweep every consumer string tied to milk for claims the moves could invalidate: the JSON's insightLine/rowVerdict/expansion of all 18, page-level prologue/page_copy, `bari-web/src/lib/comparisons/milk-page-data.ts` adapter prose, hashvaot hub/featured cards mentioning milk, SEO/FAQ files. For each affected claim: file:line, the claim, why the move invalidates it (number/rank/superlative). DO NOT EDIT ANY COPY — the list goes to the owner (his rewrite project owns those strings). If a claim becomes FACTUALLY FALSE post-refresh, mark it CRITICAL in the list (the owner decides: fix via his freeze lane before/with this deploy).

**Gates (all mandatory):** G1-G8 `run_gates.py` on the candidate with `--baseline` = THIS WORKTREE's committed milk_frontend_v1.json (= origin/master copy, never C:\Bari's); pre-existing-only failures must be shown byte-identical on the baseline run. `conformance.py --categories milk`. Score==trace check on all 18. `npx tsc --noEmit` + `npm run build` in `bari-web\` after swapping the JSON (npm ci first). C10 diagnostic re-run against the CANDIDATE: 0 drift (and state what C10 compares against — if its baseline dir also needs re-pointing after refresh, say so, report-only).

## 3. Boundaries
- OFF ban absolute. No invented data — every number from real traces. NO copy edits (Step D report-only; freeze). No engine changes (flags-off current engine IS the target). No push/PR/deploy.
- You are the EXECUTOR — do NOT spawn subagents. Leave the tree clean (gitignored droppings ok).

## 4. Return
`tasks\returns\P477_contract.md` (NOT P477_return.md). Step-A reproduction table, root-cause paragraphs, FULL 18-row movement table with Rule-5 distribution (grade dist old vs new, stdev, most_common), copy-impact list with severities, every command + exit code + env prefixes, real sha256s. Self-gate: `python 03_operations\validators\validate_return.py --md tasks\returns\P477_contract.md --root C:\bari_wt_t468` exit 0 (PowerShell, not Git Bash). Commit code + contract. Propose RETURNED (or BLOCKED per Step A).
