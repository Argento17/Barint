# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results/
/e2e/vision-out/
/.vision-in/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Agent OS lives ONLY at C:\Bari — never snapshot it into the website (TASK-131)
/Bari/

# Product Dossier dev data (TASK-611 / PD-3) — derived cache synced from the
# compiler's own gitignored, UNCOMMITTED sample output (03_operations/
# product_dossier/dossiers_sample). Regenerate via `npm run sync:dossiers`;
# never hand-edited, never committed (R-D: no committed baseline pre-parser-fix).
/src/data/dossiers_generated/
